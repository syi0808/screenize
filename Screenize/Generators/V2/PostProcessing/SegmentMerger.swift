import Foundation

/// Merges short or similar adjacent scene segments.
/// Implementation pending — Step 4 of V2 migration.
struct SegmentMerger {
    static func merge(
        _ path: SimulatedPath,
        settings: MergeSettings
    ) -> SimulatedPath {
        path
    }
}
