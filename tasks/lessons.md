# Lessons

- When wiring SwiftUI settings entry points on macOS, check API availability before using `SettingsLink`; macOS 13 requires the legacy preferences selector instead of the macOS 14 settings selector.
